<Note>
**Important for Windows Server 2016 Users**  
If you are using **Windows Server 2016**, `curl` is **not installed by default**.  
Follow the steps in the [cURL Installation](../integrations/config_curl) section of this guide.
</Note>

## Installing Windows agent

### 1. Check pre-installation requirements

    * Compatible with **Windows Server version 2016** or higher
    * **The UTMStack agents communicate over ports 9000 and 50051**. Please make sure these ports are open.

### 2. To install or uninstall the UTMStack agent, open a Windows PowerShell terminal as "Administrator" and run the following command:


<img
  height="200"
  src="/images/integrations/windowsagent/step2.png"
  alt="Windows Agent"
/>


<Warning>
    The following command contains sensitive information, don't share it.
</Warning>

### 3. Run the following command:

<Tip>
     Open a Windows PowerShell terminal as "Administrator"
</Tip>

<Warning>
    Agent installation commands for operating systems **must be copied directly from your UTMStack instance**, under the **Integrations** section.  
    This ensures that all tokens, identifiers, and configuration parameters are accurate and specific to your environment.
</Warning>

#### Example:

<img
  height="200"
  src="/images/integrations/windowsagent/step5.png"
  alt="Windows Agent"
/>
