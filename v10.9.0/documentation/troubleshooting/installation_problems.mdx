# Installation Problems

## Introduction

This guide provides troubleshooting steps for common installation and deployment issues encountered with UTMStack version 10.9.0. It includes information on system requirements, installation procedures, and configuration recommendations to ensure a smooth setup process.

## System Requirements

Before installing UTMStack, ensure that your system meets the necessary hardware and software prerequisites. Detailed system requirements can be found in the [System Requirements Documentation](https://documentation.utmstack.com/v10/Installation/system_requirements).

## Preparing for Installation

To prepare your Ubuntu Server for UTMStack installation, follow these steps:

1. **Update Packages List**:
   ```bash
sudo apt update
   ```

2. **Install WGET**:
   ```bash
sudo apt install wget
   ```

3. **Download the Installer**:
   ```bash
wget http://github.com/utmstack/UTMStack/releases/latest/download/installer
   ```

4. **Change to Root User**:
   ```bash
sudo su
   ```

5. **Set Execution Permissions**:
   ```bash
chmod +x installer
   ```

## Running the Installation

Execute the installer without parameters to begin the installation process:

```bash
./installer
```

Once installed, use `admin` as the username and the password generated during installation, which can be found in `/root/utmstack.yml`. Access the login page using HTTPS in front of your server name or IP.

## Required Ports and Firewall Recommendations

Ensure the following ports are open and properly configured:

- **22/TCP**: Secure Shell (SSH) - Recommended to allow only from admin workstation.
- **80/TCP**: UTMStack Web-based GUI Redirector - Recommended to allow only from admin and security analyst workstations.
- **443/TCP**: UTMStack Web-based GUI - Recommended to allow only from admin and security analyst workstations.
- **9090/TCP**: Cockpit Web-based GUI for Servers - Recommended to allow only from admin workstation.

Additional ports may be required for UTMStack's integrations. Follow the security recommendations in the integration guide if available.

## Frequently Asked Questions

- **Is UTMStack based on Grafana, Kibana, or a similar tool?**
  No, UTMStack is built from the ground up as a simple and intuitive SIEM/XDR.

- **Does UTMStack use ELK for log correlation?**
  No, UTMStack's correlation engine is custom-built for real-time data analysis.

- **What is the difference between the Open Source and Enterprise version?**
  The enterprise version includes features beneficial to enterprises and MSPs, such as enhanced support.

## Summary

This guide provides essential steps and recommendations for installing UTMStack on Ubuntu Server. By ensuring system requirements are met and following the installation and configuration instructions, users can effectively deploy UTMStack and address common installation issues. For further assistance, refer to the [Support Page](https://support.utmstack.com).

For more detailed guides and resources, visit the [UTMStack Documentation](https://documentation.utmstack.com/v10/Getting%20started/introduction).
